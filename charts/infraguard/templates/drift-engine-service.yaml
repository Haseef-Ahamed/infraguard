apiVersion: v1
kind: Service
metadata:
  name: drift-engine
  namespace: drift-engine
  labels:
    app: drift-engine
spec:
  selector:
    app: drift-engine
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
